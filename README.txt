
Copy of winjcom from http://www.engidea.com/blog/informatica/winjcom/winjcom.html

This copy has been modified in the renaming of Java packages and C source files.

This copy is staged here for rewriting HANDLE/FD jint's to jlong's (done), and testing under 64 bit windows (tbd).

TODO: TEST

 * Build the DLL using the "make dll" script.
 * (Copy the DLL into PATH for java runtime access - the "make dll" script copies into "c:\windows")
 * Write and run a test program using the "winjcom" package and evaluate

